3.20 开始研究、构思
3.25 注册功能实现 难点：1、没有学过数据库 所以需要自学 2、web-server要将从前端获取的信息，组成字典，再变成字符串传到数据处理器 3、对session的理解
3.29 实现加减法功能 难点：1、自学ajax 2、修改socket语句，服务器两层while循环，一层服务器循环，一层通讯循环，同时要判空，以检测客户端已经断连  3、PS:form表单会自己提交一个请求，在表单里的button按钮不要设置用ajax传参到某个路由，因为form表单会直接提交get方法的请求。
3.31 实现管理员上传文件 过程：通过ajax的Formdata传输文件到web服务器，然后通过socket传输到数据处理器。 重点：需要先发送头文件信息给数据处理器，让数据处理器知道要接受几次。
4.1 加上用户退出功能
//要记得将文件列表file_list放在数据处理器上，放在web服务器上每次重开就没了
4.3 文件下载功能 过程：js设置download函数发生错误，查询以后发现download是html5内置的一个属性。python 全局变量 global的使用;列表用append，会保存修改，因为这是一种引用
                难点：python file.read()的使用。在read一次过后，文件指针就会指到最后去，这样再读的话就啥也读不到了
4.5 文件夹系统ing 难点：1、ajax不会自己解析返回的html，需要在success函数里用$('body').html(result) 2、flask模板for循环的使用以及jinja2模板过滤器的使用（获取列表长度）
4.6 新建文件夹完成、返回上一层完成、完成查看本层下的文件夹信息（后期仍需要改进）过程：html里 search是内置属性，不能设置函数名为search
4.7 修补bug

